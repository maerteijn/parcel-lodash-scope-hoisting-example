import _ from "lodash"

document.addEventListener("DOMContentLoaded", event => {
    const element = document.getElementById("updateme")
    element.innerHTML = ("Hello there! When you can read this all went well, so no error occured")
})
